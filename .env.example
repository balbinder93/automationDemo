# Copy to an untracked local .env file only when your launcher loads environment files.
# Do not put real credentials in this file or commit a populated .env file.
BASE_URL=https://qa.example.test
BROWSER=chrome
HEADLESS=true
EXECUTION=local
REMOTE_URL=
TEST_USERNAME=
TEST_PASSWORD=
